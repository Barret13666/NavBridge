# NavBridge

Android-приложение для приборки на базе ESP32. Делает четыре вещи:

1. Берёт местоположение телефона через `FusedLocationProviderClient` (тот же
   API, которым пользуется само приложение Google Maps — само выбирает
   лучший источник: реальный GPS, Wi-Fi или вышки сотовой связи) и шлёт его
   как NMEA-сообщения (`$GPRMC` + `$GPGGA`) по UDP на приборку — на тот же
   IP:порт (по умолчанию 10110), что слушает приборка.
2. Считает маршруты для кнопки "GO" на экране навигации приборки —
   полностью офлайн, через отдельно установленное приложение **BRouter**,
   без какого-либо своего сервера. На стороне приборки адрес телефона
   узнаётся автоматически (из тех же GPS/UDP-пакетов) — вручную ничего не
   прописывается.
3. Передаёт **подсказки поворотов** — те самые, что BRouter считает вместе с
   маршрутом. Приборка рисует стрелку манёвра и расстояние до него.
4. Ищет адреса для кнопки с лупой на экране навигации — через открытый
   геокодер **Photon**, онлайн, без ключей и регистрации.

Маршрутизация, подсказки и поиск используют один и тот же UDP-порт **10111**
и отвечают на запросы приборки автоматически, пока запущена служба.

## Что нового в этой версии

- **Настройки** (кнопка на главном экране). Туда переехал выбор профиля
  маршрута, там же язык интерфейса и настройки подсказок поворотов. Ничего
  сохранять не надо — всё применяется сразу, и служба перечитывает настройки
  на каждом запросе, так что смена на ходу действует со следующего поворота.
- **О NavBridge** — что это, какие механизмы использует и что ещё надо
  установить (в первую очередь BRouter с офлайн-данными для своего региона).
- **Язык: английский по умолчанию**, русский переключается в настройках.
  Выбор пишется как per-app locale, а не наследуется от системы: прошивка,
  протокол и логи проекта на английском, и интерфейс, который с ними молча
  расходится, поддерживать труднее.
- **Звук и вибрация на поворотах.** Приборка присылает `TCU1` (см. блок
  подсказок в `gps_nav.cpp`), приложение произносит манёвр через TTS,
  вибрирует и/или пикает. Решает приборка, а не телефон, поэтому услышанное
  не может разойтись со стрелкой на экране. Требует прошивку с поддержкой
  `TCU1`; со старой прошивкой всё остальное работает как раньше.

## Как ведут себя подсказки поворотов

Мелочи, которые не видно в настройках, но которые и определяют, удобно ли
этим пользоваться на ходу.

Подсказки объявлены системе как **навигационные**, а не как музыка. Из-за
этого Bluetooth-гарнитура шлема и автомагнитола понимают, что это подсказка:
играют её по правильному каналу и **приглушают** трек на пару секунд, а не
останавливают его. Музыка продолжается сама, без нажатий.

**Новая подсказка перебивает старую.** Если предыдущая ещё договаривает, а
момент уже другой — важен новый. Иначе можно услышать «через триста метров
налево», уже находясь в повороте.

**Расстояния округляются до произносимых.** «Через 300 метров», а не «через
287 метров» — точная цифра здесь бесполезна, а слушать её дольше.

**Вибрация разная по смыслу:** один длинный сигнал — налево, два коротких —
направо, тройной — прибытие. Это различимо через куртку, не глядя на экран,
и работает, когда музыка в наушниках громче подсказки.

**Дублирование на браслет и часы.** Каждая подсказка дополнительно
показывается уведомлением — Mi Band, Amazfit и умные часы ничего не знают об
этом приложении, они просто повторяют уведомления телефона, так что это
единственный способ вывести подсказку на запястье. В уведомлении одна строка:
направление одним словом и расстояние — «Налево · 300 м», у самого поворота
«Налево · сейчас». Оттенки (плавно, резко, по развилке) сведены к простому
«налево»/«направо»: на браслете важна сторона, а крутизну поворота вы и так
увидите с дороги. Для кольца добавляется номер съезда.

На самом телефоне это уведомление беззвучное и без вибрации — звук и вибрацию
уже дают пункты выше, дублировать их незачем. Браслет вибрирует сам, когда
получает уведомление, и именно эта вибрация чувствуется, когда телефон в
кармане. Старая подсказка заменяется новой, а не копится стопкой, и сама
исчезает примерно через минуту: подсказка верна, только пока поворот впереди.

**Кнопка «Проверить подсказку»** в настройках проигрывает настоящую
подсказку тем же путём, что и на маршруте. Смысл именно в этом: громкость,
голос и то, попадает ли звук в гарнитуру, иначе выяснялись бы только на
перекрёстке.

## Маршрутизация (BRouter)

Для кнопки "GO" на карте приборки (тап по точке → маршрут) нужно отдельно
установленное приложение **BRouter** — оно бесплатное, с открытым кодом,
считает маршруты полностью офлайн и умеет работать в любой точке мира без
своего сервера. NavBridge само его не тянет за собой (это отдельное
Google Play/F-Droid приложение) — только обращается к нему за расчётом.

Настройка (один раз):

1. Установите **BRouter** — [Google Play](https://play.google.com/store/apps/details?id=btools.routingapp)
   или [F-Droid](https://f-droid.org/packages/btools.routingapp/).
2. Откройте BRouter, зайдите в его меню загрузки картографических данных
   ("Download Manager" / "Manage") и скачайте квадраты 5×5° для тех
   районов, где реально будете ездить (например, для Москвы и области —
   один-два квадрата). Это не весь мир и не вся страна — несколько
   мегабайт на квадрат, качается за пару минут. Когда поедете в новое
   место — так же докачайте нужный квадрат там, через тот же экран BRouter.
3. В NavBridge на главном экране выберите профиль маршрута (Электротранспорт/
   велосипед, Автомобиль или Пешком) — под тип вашего "веска" обычно подходит
   первый вариант. Профиль можно менять и во время работы службы, без
   перезапуска.
4. Дальше ничего вручную настраивать не нужно — как только вы нажимаете
   **Старт** в NavBridge, оно параллельно с отправкой координат слушает
   запросы на маршрут от приборки (UDP-порт 10111) и отвечает на них через
   BRouter.

Если на приборке при нажатии GO появляется ошибка вида "route: position not
mapped in existing datafile" — это BRouter говорит, что для этой точки нет
скачанных данных: откройте BRouter и докачайте нужный квадрат. Ошибка
"BRouter app not installed or service unreachable" — само приложение
BRouter не установлено или ещё не запускалось ни разу (откройте его хотя бы
один раз после установки).

## Подсказки поворотов

Отдельно включать нечего: NavBridge запрашивает подсказки у BRouter вместе с
маршрутом и отправляет их приборке следом за ломаной. Поддерживаются все
манёвры, которые различает BRouter — повороты трёх степеней крутизны в обе
стороны, развороты, развилки ("держаться левее/правее") и круговое движение.

Расстояние до поворота приборка меряет **по самому маршруту**, а не по
прямой, и пересчитывает на каждый фикс. Это сделано на приборке намеренно:
BRouter отдаёт расстояние, зафиксированное на момент построения, и оно верно
ровно ту секунду, когда вы миновали предыдущий манёвр.

Команды "ехать прямо", "прямая между точками" и "вы сошли с маршрута" не
показываются: первые две — шум, а отклонение от маршрута приборка определяет
сама и заметно быстрее.

**Единственное условие — профиль BRouter.** Подсказки генерируются, только
если в активном профиле задан `priorityclassifier`. В современных стоковых
`trekking.brf` и `car-fast.brf` он есть, в старом скачанном профиле может не
быть. Проверять вручную ничего не нужно: после каждого построения маршрута
приборка показывает одну из строк

```
route: 12 turn hints from BRouter
route: no BRouter hints (profile lacks priorityclassifier?)
```

Вторая означает, что маршрут построен, но стрелок от BRouter не будет —
обновите профиль. Совсем без подсказок приборка при этом не остаётся: она
вычислит повороты из геометрии ломаной сама. Это грубее — круговое движение
и развилки так не различить, а поворот дороги на перекрёстке, где вы едете
по главной, попадёт в подсказки лишним.

## Поиск адресов (Photon)

**Ничего дополнительно устанавливать не нужно.** Поиск работает через
[Photon](https://photon.komoot.io) — открытый геокодер по данным
OpenStreetMap, который держит Komoot. Ни ключа, ни регистрации, ни
отдельного приложения: NavBridge обращается к нему напрямую по HTTPS.

Единственное требование — **интернет на телефоне** в момент поиска. Это
отличает поиск от маршрутизации: BRouter считает офлайн, Photon — нет. Уже
построенный маршрут при потере связи продолжает работать, а вот новый адрес
без интернета не найдётся.

Как пользоваться:

1. На экране навигации приборки нажмите кнопку с **лупой** (слева сверху,
   рядом со стрелкой "назад").
2. Наберите адрес или название места. Список появляется сам, через секунду
   после того, как вы перестали печатать — жать ничего не надо. Минимум
   3 символа.
3. Клавиатура переключается кнопками в нижнем ряду:
   - **ABC** — латиница
   - **РУС** — кириллица
   - **123** — цифры, знаки и польские буквы (ĄĆĘŁŃÓŚŹŻ)

   Выбранный алфавит запоминается между поисками и переживает перезагрузку.
   Цифровая раскладка не запоминается — это временный заход за номером дома,
   а не язык.
4. Тапните нужную строку — панель закроется, карта встанет на найденную
   точку, на ней появится метка назначения. Клавиша **OK** открывает первый
   вариант из списка.
5. Дальше как обычно: нажмите **GO**, и маршрут посчитает BRouter.

Поиск учитывает, где вы находитесь: при одинаковых названиях ближайшее
показывается первым. Регистр не важен, диакритику можно не набирать —
"Wroclaw" находит "Wrocław".

Одинаковые по названию, но разные объекты (классический случай — "ВДНХ":
станция метро, парк, выставочный центр и район) NavBridge различает сам:
дубликаты одного и того же места отбрасываются, а к остальным дописывается
район, улица или тип объекта, чтобы строки не выглядели одинаковыми.

Результаты, которые приборка физически не сможет отрисовать — иероглифы,
хангыль, арабица, — отсеиваются на телефоне и до неё не доезжают.

Маршрут не строится автоматически по тапу на результат — это сделано
намеренно: выбрать не тот вариант и сразу запустить расчёт хуже, чем нажать
GO лишний раз.

Если на приборке вместо списка появляется текст — это ответ сервера:
"no results" — ничего не найдено, попробуйте иначе; "rate limited, wait 15s"
— слишком часто, подождите; "network: ..." — нет интернета на телефоне;
"no response from phone (app running?)" — не запущена служба NavBridge.

## Установка

Скачайте последний `NavBridge-*.apk` со страницы
[Releases](../../releases), откройте файл на телефоне и установите —
Android попросит разрешить установку "из неизвестных источников" для того
приложения, через которое вы открыли файл (Файлы, браузер и т.п.), это
нормально для APK не из Google Play.

### На стороне приборки

В папку скетча рядом с `gps_nav.cpp` нужно положить два файла шрифтов.
Arduino IDE подхватит их сам, настраивать ничего не требуется.

| Файл | Зачем |
|---|---|
| `ui_font_nav14.c` | текст поиска: латиница с диакритикой, вся кириллица (включая украинскую), «№», кавычки-ёлочки |
| `ui_font_turn40.c` | пиктограммы манёвров |

Без первого названия и подписи клавиш отрисуются пустыми прямоугольниками —
стандартные шрифты LVGL содержат только ASCII. Без второго не будет стрелок
поворотов.

Если добавлять шрифт поиска не хочется, поставьте
`NAV_USE_CUSTOM_SEARCH_FONT 0` в `gps_nav.cpp` — поиск продолжит работать,
но нелатинские символы будут видны как прямоугольники.

## Использование

1. Откройте **NavBridge**.
2. Введите IP вашего ESP32 (тот же, что показан на экране навигации) и порт
   (по умолчанию `10110`, менять не нужно). Выберите профиль маршрута (см. раздел выше).
3. Нажмите **Старт** — приложение спросит разрешение на геолокацию
   (разрешите), и на Android 13+ — разрешение на уведомления (тоже
   разрешите, иначе не будет видно постоянного уведомления о работе службы).
4. Дальше приложение работает в фоне (с уведомлением, в котором есть кнопка
   **Стоп**) и шлёт координаты каждые ~3 секунды, пока не нажмёте Стоп, а
   заодно отвечает на запросы маршрута, подсказок и поиска от приборки.
   Экран телефона можно выключать — фоновая служба (foreground service)
   продолжит работать.

## Ограничения / что стоит знать

- Приложению нужны **Google Play Services** (для `FusedLocationProviderClient`)
  — на обычных Android-телефонах это есть всегда, но на "degoogled"
  прошивках (GrapheneOS и т.п.) работать не будет.
- Точность зависит от того, что смог отдать `FusedLocationProviderClient` —
  на улице обычно это GPS (несколько метров), в помещении — по Wi-Fi/вышкам
  (от ~30 до нескольких сотен метров), как в обычных картах на телефоне.
- Координаты отправляются примерно раз в 3 секунды. Приборка рассчитана
  именно на этот интервал: от него зависят её пороги "связь пропала" и
  "фикс устарел", и от него же — как часто обновляется расстояние до
  поворота.
- Приложение не подписано релизным ключом — это debug-сборка, для личного
  использования этого достаточно.
- BRouter считает маршруты офлайн, но у него есть предел по прямой между
  точками — около 150 км (время расчёта растёт квадратично с расстоянием).
  Для городских/районных поездок этого с большим запасом хватает.
- Расчёт маршрута идёт на CPU телефона в момент нажатия GO — первый раз для
  новой области может занять несколько секунд (плюс BRouter должен успеть
  прочитать её сегмент с диска).
- Публичный сервер Photon — бесплатный и без гарантий доступности; при
  чрезмерной нагрузке запросы урезаются. NavBridge держится в разумных
  рамках: приборка ждёт секунду тишины после последней нажатой клавиши и не
  переспрашивает то, что уже спросила, а приложение не делает больше одного
  запроса в 1.2 секунды. Если сервер всё же ответит отказом, NavBridge на
  15 секунд перестаёт к нему обращаться вообще — повторный стук в закрытую
  дверь это то, за что блокируют, а не ограничивают. За поездку набегают
  десятки запросов, а не тысячи.
- Если захочется независимости от публичного сервера, Photon ставится и
  локально (Apache-2.0) — тогда достаточно поменять `PHOTON_BASE` в
  `PhotonClient.kt` на адрес своего сервера.
- Поиск отдаёт максимум 8 результатов; на экране приборки помещается 4.

## Лицензия

MIT — см. [LICENSE](LICENSE).

Данные поиска и маршрутов — OpenStreetMap,
[ODbL](https://www.openstreetmap.org/copyright).
